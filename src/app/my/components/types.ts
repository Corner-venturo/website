export interface Achievement {
  icon: string;
  label: string;
  color: string;
  rotate: string;
  isFounder?: boolean;
}

export interface QuickAction {
  title: string;
  subtitle: string;
  icon: string;
  color: string;
  accent: string;
  href: string;
}
